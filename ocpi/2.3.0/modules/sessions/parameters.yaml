openapi: 3.1.0
components:
  parameters:
    SessionIdParameter:
      in: path
      name: session_id
      description: Session.id of the Session object to retrieve or update.
      required: true
      schema:
        type: string
        maxLength: 36
